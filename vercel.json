{
  "buildCommand": "bun x biome check --no-errors-on-unmatched && bun x tsc --noEmit && bun x vite build",
  "installCommand": "bun install",
  "env": { "NODE_ENV": "production" },
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
