{
  "buildCommand": "bun x biome format --write && bun x biome check && bun x tsc --noEmit && bun x vite build",
  "installCommand": "bun install",
  "env": { "NODE_ENV": "production" },
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
